apiVersion: apps/v1
kind: Deployment
metadata:
  name: pictochat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pictochat
  template:
    metadata:
      labels:
        app: pictochat
    spec:
      containers:
      - name: pictochat
        image: ckings00/dat515-pictochat:1.2
        env:
        - name: MYSQL_HOST
          value: mysql
        - name: MYSQL_USER
          value: "me"
        - name: MYSQL_PASSWORD
          value: "me"
        - name: MYSQL_DB
          value: "todos"
        - name: LISTEN_PORT
          value: "30080"
        ports:
        - containerPort: 30080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 80
          initialDelaySeconds: 3
          periodSeconds: 3
